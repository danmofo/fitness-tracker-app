import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
    label: {
        marginBottom: 8
    },
    fieldError: {
        color: 'red'
    },
    input: {
        borderWidth: 1,
        borderColor: '#CCC'
    }
});